# Supabase Configuration
# Get these values from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# JWT Configuration
# Generate a secure random string for JWT signing
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_random

# CORS Configuration
# Comma-separated list of allowed origins (e.g., https://yourdomain.com,https://app.yourdomain.com)
# Use * for development or to allow all origins (not recommended for production)
ALLOWED_ORIGINS=https://taeshigee-production.up.railway.app 